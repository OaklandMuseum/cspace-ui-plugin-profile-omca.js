import { defineMessages } from 'react-intl';

export default {
  inputTable: defineMessages({
    condition: {
      id: 'inputTable.conditioncheck.condition',
      defaultMessage: 'Condition',
    },
  }),
};
